#include <stdio.h>

int main(void) {
  int numero = 0;  
  printf ("Digite um número: " );
  scanf ("%d", &numero);
  printf ("Seu número: %d \n", numero);
  printf("Seu sucessor: %d \n", numero + 1);
  return 0;
}